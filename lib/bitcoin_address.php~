<?php
function bitcoin_address_pair(){
	$script =  __DIR__."/botg.sh";
	exec($script, $result);
	$result = array_map('trim', $result);
	$priv = $result[2];
	$pub = $result[4];
	return array($priv, $pub);
  }

print_r(bitcoin_address_pair());
